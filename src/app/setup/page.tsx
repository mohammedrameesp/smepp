/**
 * @file page.tsx
 * @description Setup wizard page for new organization onboarding
 * @module setup
 */

import { SetupWizardClient } from './components/SetupWizardClient';

export default function SetupPage() {
  return <SetupWizardClient />;
}
